# Local MongoDB for remotelymatch development.
# Usage:
#   docker compose up -d
#   Set in backend/.env: MONGODB_URI=mongodb://127.0.0.1:27017/remotelymatch
#   npm run dev
#
# Production on Render: use MongoDB Atlas (free M0), not this container.
# Render runs one web container — it does not host a separate Mongo service via compose.

services:
  mongo:
    image: mongo:7
    container_name: remotelymatch-mongo
    restart: unless-stopped
    ports:
      - '27018:27017'
    volumes:
      - remotelymatch_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: remotelymatch
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  remotelymatch_mongo_data:
